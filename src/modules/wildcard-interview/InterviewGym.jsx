// ⚡ WILDCARD — Interview Intelligence Layer
// Top build priority. AI mock interviews → medal engine → ReadinessSignal
// written to the spine (ProfileContext). See architecture doc Section 5.1.
//
// import { useProfile } from '../../shared/context/ProfileContext.jsx'
// import { submitInterviewSession } from '../../api/mockApi.js'

export default function InterviewGym() {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-4">⚡ Interview Intelligence Layer</h1>
      <p className="text-gray-500">
        TODO: question flow → response capture → mockAI scoring → medal → write ReadinessSignal to spine.
      </p>
    </div>
  )
}
