// Thin onboarding screen — Work Animal traits, feeds Navigator + Career Mirror.
// Kept deliberately light per architecture doc Section 5.6.
export default function WorkAnimalOnboarding() {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-4">Work Animal Onboarding</h1>
      <p className="text-gray-500">TODO: single-step trait quiz, writes to candidate profile.</p>
    </div>
  )
}
